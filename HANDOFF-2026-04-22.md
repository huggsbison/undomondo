# Session Handoff — 2026-04-22

## What got done this session

1. **Stamp-top-right rollout** to all 19 year pages (archive/2005-2008.html + tumblr/2007-2021.html).
2. **archive/post.html** patched: cream site-nav (replacing black one) + embed CSS + buildEmbeds() JS + wrapping post.content in buildEmbeds().
3. **tumblr/post.html** patched same way.
4. **buildEmbeds() upgraded** — now scans raw contentHtml (not just a[href]) and includes old WP Flash `moogaloop.swf?clip_id=NUM` pattern for Vimeo. Strips dead `<object>/<embed>` blocks.
5. **Root index swap** — old `index.html` preserved as `index-old2.html`; new root is landing-v2 content with `../assets/` paths fixed to `assets/`.
6. **index.html content edits** — Mondo Times linked to undomondo.substack.com; Ghost World Transmissions moved under new MIXES header; Terminal Optimism caption reworded with Spotify + Apple Music links; ARCHIVE header added above archive CTA; all 5 section headers unified to `text-3xl md:text-4xl`.

## Critical deploy issue discovered

`git status` showed many files are **untracked** (never committed):
- `tumblr/*` (all of it — post.html, index.html, posts.json, 2007-2021.html)
- `archive/2005-2008.html`
- `landing/`, `assets/`
- `archive/post.html` was MODIFIED but unstaged

User needs to:
```
cd ~/Documents/CLAUDE/undomondo-fresh
git add -A
git commit -m "rebuild: cream nav, stamp-top-right, embeds, landing swap"
git push
```

Black-nav regression, embeds missing, stamp-misplaced — all symptoms of Netlify serving pre-commit versions.

## Pending for next session (PRIORITY ORDER)

| # | Task |
|---|------|
| **#24** | **RE-SCRAPE TUMBLR BACKUP** — biggest impact. Original scraper only kept `.caption`, threw away `<iframe>`. Backup at `OLD BUILDS/Tumblr.zip → posts.zip → html/*.html` has the full YouTube/Vimeo iframes for 2,337 posts. Walk all 4,108 files, extract iframe src + caption, rebuild posts.json. Recovers: 1,856 YouTube + 397 Vimeo embeds. |
| #16 | Style `tumblr/index.html` (tumblr hub) like archive hub — cream bg, stamp top-right, new site-nav. |
| #21 | WP single-post mp3 regression — `<div class="download">` song name + mp3 link missing. Posts.json still has ~491 download divs. Test with id=131 (Yid Vicious). |
| #22 | Vimeo embed visual — black letterbox strip at top. Check aspect ratio in `.embed-wrap`. |
| #20 | Orphan Tumblr posts (247, SHAM 69 style — channel-only link). Backup doesn't have the video either. Needs Tumblr API with a key, or YouTube search by title. Lowest priority. |

## Important workflow note

**User deploys via Netlify web UI (drag-drop), NOT git.** Ignore "git commit" / "untracked files" advice. Edits in `~/Documents/CLAUDE/undomondo-fresh/` go straight to the Netlify drop zone when user zips+uploads.

## Confirmed working (just needs deploy)

- buildEmbeds auto-embeds YouTube, Vimeo (modern + Flash moogaloop), SoundCloud, Mixcloud URLs from post content.
- Confirmed regex matches on Marc Bolan (id=303407913), Sunday Reblog (id=655), Ljudbilden (id=741).
- `type:"link"` tumblr posts (SHAM 69, Warzone, Negative Approach, etc.) have NO video URL in their content — need backfill per #20.

## Files preserved as backups

- `index-old.html` — the very first old landing
- `index-old2.html` — the prior committed root index (before the swap)
- `landing/index-v2.html` — original source of current root

## Key paths

- `~/Documents/CLAUDE/undomondo-fresh/` — working tree
- `index.html` — new root (swapped from landing-v2)
- `archive/post.html`, `tumblr/post.html` — single-post templates with buildEmbeds
- `archive/posts.json` (646 WP posts), `tumblr/posts.json` (tumblr posts)
